server {
    listen 80;
    server_name ranking.seudominio.com.br;   # <- troque pelo seu domínio ou IP

    # Redireciona HTTP → HTTPS (descomente depois de configurar SSL)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 90;
    }
}

# HTTPS (ativar depois de: certbot --nginx -d ranking.seudominio.com.br)
# server {
#     listen 443 ssl;
#     server_name ranking.seudominio.com.br;
#     ssl_certificate /etc/letsencrypt/live/ranking.seudominio.com.br/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/ranking.seudominio.com.br/privkey.pem;
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }
# }
